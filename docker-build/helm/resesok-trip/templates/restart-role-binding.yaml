apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "resesok-trip.serviceAccountName" . }}-restart-role-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "resesok-trip.serviceAccountName" . }}-restart-role
subjects:
  - kind: ServiceAccount
    name: {{ include "resesok-trip.serviceAccountName" . }}-restart-account
