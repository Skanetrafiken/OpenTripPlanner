apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: {{ include "resesok-trip.fullname" }}-canary
spec:
  host: "{{ .Values.global.virtualService.name }}-canary"
  trafficPolicy:
    loadBalancer:
      simple: ROUND_ROBIN